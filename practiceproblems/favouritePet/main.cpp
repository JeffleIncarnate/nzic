#include <iostream>
#include <bits/stdc++.h>
#include <string>

using std::string, std::cin, std::cout;

int main() {
    string input;

    getline(cin, input);

    for (int i = 0; i < input.length(); ++i) {
        if (input[i] == ' ') {
            continue;
        }

        
    }
    
    cout << input;
}